pipeline {
    agent any

    stages {
        stage('Check Node.js Version') {
            steps {
                echo 'Checking Node.js version...'
                sh 'node --version'
            }
        }

        // Other stages...
    }

    // Post section...
}
